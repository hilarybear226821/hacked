# Core dependencies
scapy>=2.5.0
pyyaml>=6.0
requests>=2.31.0

# Bluetooth scanning
pybluez>=0.23

# Note: HackRF support requires system packages (libhackrf-dev, hackrf)
# These are installed via apt, not pip

# Web dashboard (optional)
flask>=3.0.0
flask-socketio>=5.3.0
flask-cors>=4.0.0
zeroconf>=0.131.0

# Signal processing (optional for jamming detection) and visualization
scipy>=1.11.0
matplotlib>=3.7.0

# Utilities
python-dateutil>=2.8.0
colorama>=0.4.6

# Phone integration
adb-shell>=0.4.0
websockets>=10.0
pycryptodome>=3.15.0
